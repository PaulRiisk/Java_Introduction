package errorunderstanding;

public class Main {
    public static void main(String[] args) {
    // uncomment the lines to see the errors

    //java: cannot find symbol
    //"Something is red inside the code"
    //System.out.println(a231);

    //exception in thread "main" java.lang.NullPointerException
    //something is null (doesn't have a value)
    String s = null;
    //System.out.println(s.length());

    //process finished with exit code 1
    //"finished with non-zero exit value 1"
    //never only post this. this just shows you don't know something
    //usually there should be an error log/output or an 'exception'

    }
}
